{
  "name": "supabase-rls-leak-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A reproducible Supabase row-level-security data leak, and the two-file fix, with the same test red on one branch and green on the other.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "check": "npm run lint && npm run typecheck && npm run test"
  },
  "dependencies": {
    "@electric-sql/pglite": "^0.2.17"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@types/node": "^22.10.2",
    "eslint": "^9.17.0",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.1",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=20"
  }
}
