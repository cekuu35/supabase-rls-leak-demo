{
  "name": "supabase-rls-leak-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A reproducible Supabase row-level-security data leak and its one-file fix, with the same test suite red on one branch and green on the other.",
  "scripts": {
    "test": "vitest run",
    "test:ci": "node scripts/verify-ci-tests.mjs",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "check": "npm run lint && npm run typecheck && npm run test"
  },
  "dependencies": {
    "@electric-sql/pglite": "0.5.4"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/node": "22.20.1",
    "eslint": "10.8.0",
    "typescript": "5.9.3",
    "typescript-eslint": "8.65.0",
    "vitest": "4.1.10"
  },
  "engines": {
    "node": "^20.19.0 || ^22.13.0 || >=24"
  }
}
